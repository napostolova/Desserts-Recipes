const authController = require('./auth');
const recipesController = require('./recipesController');
const recentController = require('./recentController');

module.exports = {
    authController,
    recipesController,
    recentController,
}