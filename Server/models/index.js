const userModel = require('./userModel');
const tokenBlacklistModel = require('./tokenBlacklistModel');
const recipeModel = require('./recipeModel');
const recentModel = require('./recentModel');

module.exports = {
    userModel,
    tokenBlacklistModel,
    recipeModel,
    recentModel 
}